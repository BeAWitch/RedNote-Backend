spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 192.168.56.2:8848 # nacos
      config:
        file-extension: yaml
        shared-configs:
          - data-id: redis.yaml
          - data-id: log.yaml